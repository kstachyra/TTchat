/*
 * Message.cpp
 *
 *  Created on: Jun 7, 2016
 *      Author: ks
 */

#include <string.h>
#include "Message.h"
#include <string>

/* Message::setNick(string nick)
{
	// Check if we can fit whole nick in da buffer
	if(strlen(nick.c_str()) > MESSAGE_NICK_LENGTH - 1) {
		memcpy(this->nick, nick.c_str(), MESSAGE_NICK_LENGTH);
	} else {
		memcpy(this->nick, nick.c_str(), strlen(nick.c_str()) + 1);
	}
}

string Message::getNick(void)
{
	string nick(this->nick);

	return nick;
}*/



